apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.app.configMapName }}
  labels:
    app: {{ .Values.app.name }}
data:
  PGDATA: /var/lib/postgresql/data
  TZ: {{ .Values.Timezone | quote }}
