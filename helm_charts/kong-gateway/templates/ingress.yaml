apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kong-tailscale
spec:
  ingressClassName: tailscale

  defaultBackend:
    service:
      name: kong-proxy
      port:
        number: 80

  tls:
    - hosts:
        - kong